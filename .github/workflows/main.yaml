name: 'Basic'

on: 
  push:
    branches:
      - main

jobs:
  build-test:
    name: 'Build Test Job'
    
    runs-on: ubuntu-latest

    steps:
      - name: Hello world
        shell: bash
        run: |
          echo "Hello world"

  dev:
    name: 'Dev Job'
    needs:
      - build-test
    runs-on: ubuntu-latest

    steps:
      - name: Hello world
        shell: bash
        run: |
          echo "Hello world"

  test1:
    name: 'Test Job'
    needs:
      - dev
    runs-on: ubuntu-latest

    steps:
      - name: Hello world
        shell: bash
        run: |
          echo "Hello world"

  test2:
    name: 'Test Job'
    needs:
      - dev
    runs-on: ubuntu-latest

    steps:
      - name: Hello world
        shell: bash
        run: |
          echo "Hello world"

  prod:
    name: 'Production Job'
    needs:
      - test1
      - test2
    runs-on: ubuntu-latest

    steps:
      - name: Hello world
        shell: bash
        run: |
          echo "Hello world"