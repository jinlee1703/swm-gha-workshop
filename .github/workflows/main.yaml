name: 'Basic'

on: 
  push:
    branches:
      - main

jobs:
  build-test:
    name: 'Build Test Job'
    
    runs-on: ubuntu-latest

    steps:
      - name: Hello world
        shell: bash
        run: |
          echo "Hello world"

  dev:
    name: 'Dev Job'
    
    runs-on: ubuntu-latest

    steps:
      - name: Hello world
        shell: bash
        run: |
          echo "Hello world"

  test:
    name: 'Test Job'
    
    runs-on: ubuntu-latest

    steps:
      - name: Hello world
        shell: bash
        run: |
          echo "Hello world"

  prod:
    name: 'Production Job'
    
    runs-on: ubuntu-latest

    steps:
      - name: Hello world
        shell: bash
        run: |
          echo "Hello world"